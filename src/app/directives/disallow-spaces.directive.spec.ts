import { DisallowSpacesDirective } from './disallow-spaces.directive';

describe('DisallowSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisallowSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
